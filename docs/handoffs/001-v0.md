---
title: Handoff — v0
purpose: Onboard the next session to plan 001 — state, build order, non-negotiables, traps
created: 2026-07-21
updated: 2026-09-10
plan: ../plans/001-v0.md
---

# Handoff 001 — v0

The only handoff. Pairs with [`../plans/001-v0.md`](../plans/001-v0.md).

**Read next:** plan **Part I** (§1-§5) — goal, decisions, approach, architecture, roadmap;
~5 minutes and it is the whole project. Do not start coding before reading §3.1 — it is what
keeps the gate honest. Plan Part II (§6-§12) is executable detail; open it per milestone.

## What this repo is

A harness that makes agent-generated CAx **verifiable without overclaiming**. Give it an
exported mesh; a cheap deterministic preflight catches the obviously-broken, an automatic
multi-view render is the primary gate, and a human makes the call.

One-line thesis: *coding agents write code well and reason about 3D space badly — so check
what geometry can cheaply prove, render the rest for a human in one glance, and keep the
model advisory.*

> If you ever see this described as "a deterministic verifier gates it against a
> pre-declared, machine-checkable `DesignSpec`, with a bounded VLM critique as a secondary
> pass" — **that is the killed design.** It failed its red-team. Plan §3.1.

## State (2026-08-05)

| Item | State |
|---|---|
| Remote | `https://github.com/qte77/agentic-cax-gauge` — public, `main`. Nothing in flight |
| Code | **None.** No `src/`, no `pyproject.toml`, no CI |
| Docs | Five: plan + handoff + two reference + map. Plan split into Part I (understand) / Part II (build). Landscape re-swept and CAE research complete — plan §12.1 and §12.7 both closed (PR #5) |
| Milestone | **M1 (scaffold) is next.** The docs arc is finished; the next PR should be `pyproject.toml`, not prose |
| Merging | **Checked live 2026-09-10 — the stale-check-name claim (§12.3) doesn't match the actual ruleset.** `gh api .../rulesets/19218207` shows the required context is already `CodeFactor`. Real open question instead: `bypass_actors` is empty, so it's untested whether `--admin` bypass does anything, or whether a plain squash-merge just succeeds (0 approving reviews required) once CodeFactor reports. Test on the first real PR. What's confirmed and still stands: `env -u GH_TOKEN -u GITHUB_TOKEN` is required for **any** `gh` write call in this environment (verified today creating issue #7) — both vars, or `gh` falls through to the Codespaces App token and fails with "Resource not accessible by integration" |

## Build order (do not reorder)

1. **M1 — scaffold.** `pyproject.toml` (Python 3.12 exactly), `Makefile`, CI, governance,
   and the new `.claude/rules/verification-honesty.md`. No product code.
   *Done when:* `make validate` passes green on an empty package.
2. **M2 — the honest gate.** Inside M2: `preflight.py` first (pure, zero deps, test-first
   from fixtures), then `viewer/index.html`, `browser.py`, `render.py`, `report.py`, and
   `cli.py` last. *Done when:* a human looks at a real part's render + verdict and decides.
3. **M3 — adoption.** Run `caxgauge check <part>.stl` over so101 / i3mega exported STLs;
   wire one consumer.

**v0 takes an exported STL, not a build script.** The consumers already export STL, so v0
reaches a real user with no backend dispatch, no generation and no `DesignSpec`. That is the
whole reason the scope is this small.

**Do not build the deferred items** (generation loop, VLM annotation, slicer port, GUI, CAE)
without the trigger stated in plan §5. Each has one.

## Non-negotiables (do not regress)

Design — from the distilled red-team, plan §3.1. The 2026-07-25 landscape re-sweep raised
the stakes on all of these: `jdilla1277/agentcad` now does deterministic checks + multi-view
render + human viewer on build123d, so the honesty framing is no longer *a* differentiator,
it is **the** differentiator (plan §12.7). Weakening any of the below makes this a redundant
tool, not just a worse one.

- Green is *necessary, not sufficient*. Never emit a "verified/correct" verdict from
  deterministic checks alone. The word "verified" should not appear in the report.
- No bounded auto-critique loop with revert-on-break — it selects for gaming. The VLM (if
  ever built) annotates, never gates. "No model configured" is a first-class mode.
- Clearances are two-sided windows, never min-only.
- **An unsupplied bound is `SKIP`, never `PASS`.** `--envelope` and `--volume-band` are
  optional, so the default run has no bounds — a bbox check with no envelope has checked
  nothing and must not count toward green. This is the false-confidence failure reachable
  by default rather than by gaming.
- **Preflight alone is not a shippable gate.** Green-but-blind is the failure mode the
  red-team called worse than no gauge. That is why M2 is one milestone, not two.

Substrate — verified by reading polyfetch's source, plan §9:

- Use **`localhost`, not `127.0.0.1`**, for the viewer URL. The SSRF guard is literal-IP
  only: `localhost` passes, `127.0.0.1` raises, same destination. Wastes an hour if
  rediscovered.
- **Never assert three.js state via `page.evaluate`.** Patchright's isolated world makes
  page-script globals read `undefined` even when rendering succeeded. Screenshots are ground
  truth; `page.on(...)` answers "did it load". Highest-cost trap in the integration.
- Capture `pageerror` **and** `requestfailed` **and** non-200 `response`, not just
  `console` — parse errors never reach `console`. Force a failure once to confirm the
  listeners actually fire.

Process:

- **Stub-mode is mandatory.** Every external-tool wrapper degrades to an in-memory stub that
  logs what it would have done. This keeps CI green with no build123d/Chromium installed and
  is the primary test gate.
- **TDD, behavior-level, no Gherkin.** Skip trivial tests on declarative models.
- **Do not extract a shared CAD package** across so101/i3mega/this repo yet — two instances
  is not a stable pattern (plan §12.6).

## The headline test

The *silent boolean-fusion feature loss* fixture — a feature absorbed during fusion, still
manifold, volume barely shifted — must fail a volume-band or topology-count assertion. This
is the one real defect the cheap preflight uniquely catches. **If it passes green, the
preflight is worthless.** Build that fixture and test early.

## Traps

- **~~CAE research is owed.~~ Done 2026-07-25** — [`../reference/cae.md`](../reference/cae.md)
  (filed there, not at the once-planned `docs/outlook-cae.md`: it is research evidence, and
  evidence lives in `reference/`). Two things in it change decisions. **One:** the plan's
  reason for reserving `verify/sim.py` was wrong — a stress bound is not a bbox check, and
  CAE belongs on the render side of the trust boundary. **Two:** it found empirical backing
  for the §3.1 auto-critique kill — in 1,120 FEA trials, critic agents endorsed outputs
  including errors 85-92% of the time. Cite that if anyone proposes a critique loop.
- **Coordinate frames.** CAD-frame vs machine-frame `+Y` already bit `i3mega-pipettebot`
  once and became an enforced rule there. Adopt it on day one (plan §11).
- **Verify subagent findings before acting.** The scoping session had two research agents
  die on rate limits and one return off-task text that looked like a result.
- **Unverified inference — confirm at M2.** Plan §9.1 infers from a docs table that
  `check_ssrf` is not wired into polyfetch's `client.py`. That is inference, not a read of
  `client.py`.

## Owner gates

1. **M3 rewrites `so101-biolab-automation/docs/roadmap.md:47-58`** to point here. Cross-repo
   change to a repo with its own roadmap — confirm before landing.
2. **Merge mechanics — corrected 2026-09-10.** The stale-check-name claim doesn't match the
   live ruleset (`gh api repos/qte77/agentic-cax-gauge/rulesets/19218207`: context is already
   `CodeFactor`). What's actually true: `bypass_actors` is empty on that ruleset, so it's
   unverified whether `--admin` bypass does anything, or whether a normal squash-merge just
   succeeds once CodeFactor reports (0 approving reviews required). Test on the first real
   PR rather than assuming either path. `env -u GH_TOKEN -u GITHUB_TOKEN` is still required
   for any `gh` write in this environment regardless of which merge path works.

## Decisions made — do not relitigate

Name (`agentic-cax-gauge` / `caxgauge`), standalone-repo relationship to the estate, the
STL-input contract, aggressive v0 narrowing (§5), CAE documented-only, Python 3.12, strict
TDD, and **estate-first positioning**. Rationale for each is in plan §2. Reopen only with a
concrete reason.

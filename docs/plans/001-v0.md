---
title: agentic-cax-gauge v0 — plan + handoff
purpose: One arc, one file — design, build milestones, substrate constraints, and current state, so a fresh session can execute without re-researching or re-mapping context
created: 2026-07-20
updated: 2026-09-18
validated_links: 2026-09-10
status: approved — M1 not yet started
---

**One-line thesis:** coding agents write code well and reason about 3D space badly — so
check what geometry can cheaply prove, render the rest for a human in one glance, and keep
the model advisory.

> If you ever see this described as "a deterministic verifier gates it against a
> pre-declared, machine-checkable `DesignSpec`, with a bounded VLM critique as a secondary
> pass" — **that is the killed design.** It failed its red-team. §3.1.

This repo no longer splits plan and handoff into two files (see `docs/README.md`
conventions) — this section is what `docs/handoffs/001-v0.md` used to be, merged in
2026-09-18. If you land here from an old link to that path, it no longer exists; this is it.

---

# Handoff — read this first

**Where things stand (2026-09-18).** Remote: `https://github.com/qte77/agentic-cax-gauge`,
public, `main`, nothing in flight. Code: **none** — no `src/`, no `pyproject.toml`, no CI.
The docs arc is finished (PRs #1-#9: v0 scoping + red-team, README + LICENSE, scaffold +
preflight build plan, single-arc consolidation, plan/handoff split, arc-state close, ruleset
correction, CAx/BIM landscape research). **M1 (scaffold) is next — the next PR should be
`pyproject.toml`, not prose.**

**What's next, in order:** M1 → M2 → M3 (§5 — do not reorder, each depends on the last
existing). Nothing past M3 starts without its stated trigger (Outlook table, §5).

**Source & file map** — so the next session does not have to re-map this: the M1 file table
is §6, the M2 file table + module dependency graph is §7.1, the M3 source-parts map is §8,
the polyfetch substrate map is §9, and the full estate-reuse map (exact files, line ranges,
what to copy from where) is §11. Nothing needs re-discovering; if a section here contradicts
one of those, the numbered section wins and this line should be fixed.

**Execution model — where parallel subagents/worktrees help, and where they don't.** Decided
2026-09-18, for anyone running this build with multiple subagents in git worktrees:

- **M1 (scaffold): single agent, single worktree.** It's ~10 small, mutually independent
  config files (§6) with no real coupling between them — worktree/subagent coordination
  overhead would exceed the work itself. One pass, one agent.
- **M2 (the honest gate): real parallelism, in exactly two waves** — see §7.1 for the full
  dependency graph. Wave 1 (`preflight.py` + `viewer/index.html`, zero shared dependencies)
  and wave 2 (`browser.py` + `render.py`, both depend only on wave 1's `viewer/index.html`,
  not on each other) can each run as two subagents in two worktrees concurrently.
  `report.py` and `cli.py` are integration/wiring steps that need everything before them —
  single agent, after both waves merge. **Recommended:** define `verify/types.py`
  (`PreflightResult`, `BrowserResult` as the actual dataclasses/Pydantic models) as a single
  small agent step *before* fanning out wave 1, so both waves — and the later `report.py`/
  `cli.py` step — code against a fixed contract instead of blocking on each other's merges.
- **M3 (adoption): single agent.** One integration/exploration task against real consumer
  repos, not a set of independent files — nothing to parallelize.

**Commands:**

- **Merge** — every PR needs this. CodeFactor's own dashboard shows "Not supported" for this
  repo (confirmed 2026-09-18) because there is no analyzable code yet, so its required
  status check can structurally never post before M1 ships real Python files:
  ```
  env -u GH_TOKEN -u GITHUB_TOKEN gh pr merge <N> --squash --admin --delete-branch
  ```
  **Both** token vars must be unset or `gh` (and plain `git push`) fall through to the
  Codespaces App token and fail with "Resource not accessible by integration". This is not
  the ruleset — it is an auth-fallthrough problem, confirmed by reproducing it directly.
- **If a PR was retargeted mid-flight** (`gh pr edit --base`), its ruleset evaluation may
  reject `--admin` even though a fresh PR against the same base merges fine
  (`Required status check "CodeFactor" is expected` — reproduced twice, 2026-09-18, on an
  otherwise-identical zero-status-check commit that merged cleanly before the retarget).
  **Fix:** close and reopen a fresh PR against the real base rather than retargeting.
- Once M1 ships code, re-verify whether CodeFactor starts reporting normally — that
  assumption (a `.py` file existing is enough) has not been tested yet.

**Non-negotiables (do not regress)** — from the distilled red-team, §3.1. Weakening any of
these makes this a redundant tool, not just a worse one (§12.7):

- Green is *necessary, not sufficient*. Never emit a "verified/correct" verdict from
  deterministic checks alone; the word "verified" should not appear in the report.
- No bounded auto-critique loop with revert-on-break — it selects for gaming. The VLM (if
  ever built) annotates, never gates. "No model configured" is a first-class mode.
- Clearances are two-sided windows, never min-only.
- **An unsupplied bound is `SKIP`, never `PASS`** — `--envelope`/`--volume-band` are
  optional, so a bbox check with no envelope has checked nothing and must not count toward
  green.
- **Preflight alone is not a shippable gate.** Green-but-blind is worse than no gauge —
  that's why M2 is one milestone, not two.
- Use **`localhost`, not `127.0.0.1`**, for the viewer URL (§9.1) — same destination,
  different SSRF-guard outcome; wastes an hour if rediscovered.
- **Never assert three.js state via `page.evaluate`** (§9.2) — Patchright's isolated world
  reads page-script globals back as `undefined` even on success. Screenshots are ground
  truth for "did it render"; `page.on(...)` answers "did it load".
- Capture `pageerror` **and** `requestfailed` **and** non-200 `response`, not just `console`.
- Stub-mode is mandatory for every external-tool wrapper (CI has no build123d/Chromium).
- Strict TDD, behavior-level, no Gherkin.
- Do not extract a shared CAD package across so101/i3mega/this repo yet (§12.6) — two
  instances is not a stable pattern.

**Decisions made — do not relitigate** (rationale in §2): name (`agentic-cax-gauge` /
`caxgauge`), standalone-repo relationship to the estate, the STL-input contract, aggressive
v0 narrowing, CAE documented-only, Python 3.12, strict TDD, and estate-first positioning.
Reopen only with a concrete reason.

**Owner gates (need a human, not an agent):**

1. **M3 rewrites `so101-biolab-automation/docs/roadmap.md:47-58`** to point here — a
   cross-repo change to a repo with its own roadmap. Confirm before landing.
2. **Every merge, until CodeFactor recognizes this repo** — see Commands above.

**Traps (rediscovering these costs real time):**

- CAE's own research (`reference/cae.md` §1) disproved this plan's original reason for
  reserving `verify/sim.py` — a stress bound is not a bbox check. It also found empirical
  backing for the §3.1 auto-critique kill: across 1,120 FEA trials, critic agents endorsed
  outputs — errors included — 85-92% of the time ([arXiv:2408.13406](https://arxiv.org/abs/2408.13406)).
  Cite that if anyone proposes a critique loop.
- **Coordinate frames.** CAD-frame vs machine-frame `+Y` already bit `i3mega-pipettebot`
  once and became an enforced rule there (§11). Adopt the mirror convention on day one.
- **Verify subagent findings before acting.** This project's own scoping session had two
  research agents die on rate limits and one return off-task text that looked like a result.
- **Unverified inference — confirm at M2.** §9.1's `check_ssrf` claim is inference from a
  docs table, not a read of `client.py`.

The headline test, for when you get to it (§7.3 has the full fixture list): the *silent
boolean-fusion feature loss* fixture — a feature absorbed during fusion, still manifold,
volume barely shifted — must fail a volume-band or topology-count assertion. This is the one
real defect the cheap preflight uniquely catches. **If it passes green, the preflight is
worthless.** Build that fixture and test early.

---

# Part I — Understand

## 1. Goal

**Problem.** Coding agents write code well and reason about 3D space badly. This is
structural, not a prompting deficiency ([landscape](../reference/landscape.md) §1). An agent
asked to design a part cannot be trusted to look at a render and judge whether it is right.

**Insight.** Do not ask the model to *see*. Make the geometry assert what it cheaply can,
render the rest fast for a human, and keep the model advisory.

**Outcome.** A standalone, domain-agnostic harness that makes CAx generation test-first.
`so101-biolab-automation` and `i3mega-pipettebot` are its first two consumers — both
independently built half this machinery and both explicitly defer the loop itself
(`so101-biolab-automation/docs/roadmap.md:47-58`, `:158-169`).

**Success for v0** is one sentence: *a human looks at an automatic multi-view render beside
a per-check verdict for a real estate part, and decides.* Not "the tool says correct" —
that verdict does not exist here by construction.

## 2. Locked decisions

Reopen only with a concrete reason.

| Decision | Value | Rationale |
|---|---|---|
| Repo | `agentic-cax-gauge`, package `caxgauge` | *gauge* = conformance-verification instrument; *CAx* = CAD/CAE/CAM umbrella so CAE needs no rename; *agentic* covers LLM+VLM |
| Relationship to estate | New standalone repo; so101 + i3mega are consumers | A general verification harness is a different concern from a bio-lab robot |
| v0 scope | Scaffold + honest gate + adoption on real parts (§5) | Narrowed 2026-07-23: everything not required to put an honest report in front of a human is deferred |
| Input contract | **An exported mesh (STL), not a build script** | The consumers already export STL via their own `render.py`. Taking a mesh means v0 reaches a real user with no backend dispatch, no generation, no spec |
| CAE | Documented only, not built | Research still owed (§12.1) |
| Positioning | **Estate-first.** Build for so101 + i3mega as the real consumers; `text-to-cad` interop is a later, separate question | Guaranteed users on day one, asset stays owned. v0 is identical under every positioning option |
| Testing | Strict TDD, behavior-level, no Gherkin/BDD | Estate standard; legacy `RAPID-spec-forge` BDD guidance explicitly rejected |
| Python | 3.12 exactly | Hard constraint: `build123d -> cadquery-ocp -> VTK` has no cp313 wheels |

## 3. The approach

The honest primitive is not a machine oracle. It is **making the part cheap and fast to
judge, and honest about what it does not know.** Three layers, in this order of trust:

1. **Cheap deterministic preflight (necessary, not sufficient).** build-succeeds +
   manifold/watertight + bbox + mass, plus a browser-load check. Trustworthy for real
   *global/static* failures — including silent boolean-fusion feature loss, which is
   otherwise invisible. A gate against the obviously-broken, never a certificate of
   correctness.
2. **Auto-generated multi-view render as the PRIMARY gate.** For the defect classes the
   deterministic layer is blind to (a gusset over a hole, a hole that isn't through, a
   datum-offset mating feature), the real gate is a human looking at a fast, automatic
   3-view render. The harness's job is to make that glance instant, not to replace it.
3. **VLM as an assist, not an authority.** It annotates the render to draw the human's eye.
   It never gates, never auto-loops to "green", and cannot suppress human review. The system
   is fully functional with no model configured — which is why it is deferred out of v0
   entirely (§5).

**Why this ordering, and not the reverse.** The deterministic layer catches only
global/static/foreseen properties — roughly a third of real defects — and is blind to the
dominant class. Subordinating the render (the one signal that surfaces those) to a blind
gate is inverted. Worse, a blind-but-authoritative gate with revert-on-break **selects for
gaming**: a numerically-safe hack that dodges a check survives, while an honest fix that
risks a deterministic regression gets discarded. Hence: no auto-critique loop, no
auto-revert, human-driven iteration.

**`DesignSpec` survives only in a regression framing** — small and cheap (envelope, mass, a
few genuinely-mated clearances as two-sided fit *windows*), authored once and **reused
across many regenerations**: re-run after a build123d/OCCT bump, or a component reused
across assemblies. That is the only mode where pre-declaration earns its cost, so it is
post-v0 work (§5, M4). New checks are added **reactively** per the estate compound-learning
rule, never as a speculative tolerance/manufacturability DSL.

This dissolves most of the differentiation claim — fine, since positioning is estate-first
(§2). The value is a correct, honest internal instrument, not a moat.

### 3.1 The red-team, distilled

Read this before writing code. It is what keeps the gate honest.

Reviewed 2026-07-20 by adversarial first-principles distillation; the kill case was run by an
**independent agent** in a separate context that had not seen the distillation. Calibrated
for an internal tool — moat/TAM objections voided, only frame-independent cuts applied.
Verdict: **KILL-as-scoped**, reframe adopted. Full text:
`git show 2731ae3:docs/reviews/001-wedge-red-team.md`.

**Survived.** Deterministic checks are real and trustworthy for **global, static, foreseen**
properties: build-succeeds, manifold, envelope, gross mass/volume, foreseen clearances, FDM
overhang. Notably they catch **silent feature loss in boolean fusion** — a feature absorbed
during fusion that still reports manifold with volume inside a loose band. Genuinely
valuable and otherwise invisible. A pre-declared spec earns its cost in exactly two modes:
regression across many regenerations, and offloading arithmetic humans eyeball badly.

**Did not survive.**

- **Full-taxonomy defect coverage — FALSE.** Realistic catch rate is roughly one third, and
  only for defects named *in advance*. The dominant class in agent-generated CAD is *local,
  unforeseen, sequential, or fidelity* — gusset overlapping a hole, a through-hole that
  isn't through, datum-offset mating feature, assembly-sequence blockage. Bbox/volume/
  manifold are structurally blind to all of these. Catching them needs a per-feature
  assertion, which requires already knowing where the bug will be — circular.
- **Independence — FICTION as normally deployed.** One LLM authoring spec *and* geometry
  shares blind spots: correlated failure, not independent verification.
- **"Green gauge" is worse than "no gauge" when framed as sufficient.** It licenses skipping
  the ten-second render glance for exactly the defects it cannot see.

**Goodhart paths — the gaming routes §10 tests against.** (1) *One-sided clearance*:
"clearance ≥ min" with no upper bound lets a uniformly shrunk part pass while ruining a
press fit — hence two-sided windows, never min-only. (2) *Deletion beats repair*: negative
constraints are cheapest satisfied by removing the offending feature. (3) *Mass band rewards
hollowing or filling* to land in-band regardless of intent.

**ICP — where the surviving value is real.** Not "any agent-generated part": a part
**regenerated many times against a stable, small acceptance contract**. For a one-shot
generate-a-bracket, the render glance dominates.

**WINS IFF** the spec stays small and is reused across many regenerations; green is
documented as necessary-not-sufficient and never substitutes for viewing the render; new
checks are added reactively after a real recurring defect; clearances are two-sided; and
bands stay loose enough, with an owner, to survive OCCT/build123d/slicer drift.

**LOSES IFF** the spec is written once by the same intelligence that generates and framed as
a substitute for looking at the part; coverage is claimed as broad correctness; effort goes
into a general tolerance DSL for two hobby-scale consumers and the spec rots to a
bbox+volume stub; clearance goes back to min-only; or tight bands throw false failures
across kernel bumps nobody maintains, so the gate is quietly disabled while still implying
verification.

## 4. Architecture

v0 is everything above the dashed line. Below it are **named seams, not built** — reserved
so later work slots in rather than bolting on sideways.

```text
  exported mesh (STL)                      [v0 input contract]
        |
        v
  cheap preflight        build-ok · manifold · bbox · mass · browser-load
  necessary-not-          |
  sufficient              |  fail -> structured failure, surfaced to the human
        |                 |
        | pass            |
        v                 v
  auto multi-view render  <-- the PRIMARY gate
        |
        v
  report  (render + verdict, green labelled necessary-not-sufficient)
        |
        v
  HUMAN DECIDES                            [v0 ends here]
  - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  regression mode (M4)  ·  backend dispatch (M5)  ·  generation loop,
  VLM annotation, slicer, GUI-driven CAD, CAE  (deferred, §5)
```

**Projected file layout.** Nothing below exists yet; paths are relative to repo root and
`tests/` mirrors `src/` 1:1.

| Path | Responsibility | When |
|---|---|---|
| `src/caxgauge/verify/preflight.py` | Deterministic checks. Pure — no agent, no network | M2 |
| `viewer/index.html` | Static three.js + STLLoader page with named camera presets | M2 |
| `src/caxgauge/verify/browser.py` | Headless load check — did the mesh parse and render at all | M2 |
| `src/caxgauge/verify/render.py` | Multi-view PNG capture. **The primary gate** | M2 |
| `src/caxgauge/report.py` | Assemble render + verdict into one human review artifact | M2 |
| `src/caxgauge/cli.py` | `caxgauge check MESH` — the documented command M3 adopts against | M2 |
| `src/caxgauge/spec.py`, `verify/regression.py` | Small regression-framed spec + drift diff | M4 |
| `src/caxgauge/backends/` | build123d / OpenSCAD dispatch | M5 |
| `configs/` | Example regression specs | M4 |
| `verify/slicer.py`, `annotate/`, `gui/`, `generate.py`, `loop.py` | Deferred seams | — |
| `src/caxgauge/verify/sim.py` | CAE seam, empty. Reserving still costs nothing, but **not for the original reason** — the "same as a bbox check" rationale was disproved by the CAE research ([`reference/cae.md`](../reference/cae.md) §1). CAE sits on the *render* side of the trust boundary: it informs a human, it never gates | — |

**Removed by the reframe — do not re-add.** `verify/geometry.py`'s speculative clearance/
interference DSL, and `critique/` (the bounded auto-loop with cap-3 and revert-on-break — it
selected for gaming; replaced by the deferred advisory `annotate/`).

## 5. Roadmap

Each milestone is independently shippable: stop after any one and the repo still has value.
Every item states its **done-when** so the build can self-verify and move on.
**Decide-by-default:** open decisions carry a recommended default; proceed with it
unattended, the owner overrides at a checkpoint.

**This is the one remaining-work table.** Nothing open exists outside it; §6-§11 describe
*how* each row gets done, never a second list of *what* is open.

### v0 — build now, in this order

| # | Milestone | Gate | Done when |
|---|---|---|---|
| **M1** | **Scaffold** (§6). `pyproject.toml`, `Makefile`, CI, quality gates, governance, `.claude/rules/verification-honesty.md`. No product code | agent | `make validate` passes green on an empty package; CI runs it on PR |
| **M2** | **The honest gate** (§7). `verify/preflight.py`, `viewer/index.html`, `verify/render.py`, `verify/browser.py`, `report.py`, `cli.py` — see §7.1 for the parallel-wave breakdown | agent | A human looks at a real part's report — multi-view render + per-check verdict — and decides. Green is labelled *necessary, not sufficient*; the word "verified" appears nowhere. The silent-fusion fixture fails (§7.3) |
| **M3** | **Adoption** (§8). Run the gauge over `so101` / `i3mega` exported STLs; wire one consumer | agent, then **owner** for the cross-repo roadmap rewrite | One consumer repo runs `caxgauge check <part>.stl` on its real parts from its own Makefile or docs, and its roadmap points here |

**Why M2 is one milestone, not two.** Preflight alone is *green but blind* — precisely the
false-confidence instrument §3.1 calls worse than no gauge. Shipping it standalone would
ship something dishonest by construction. The gate is only honest once a human can see the
render beside the verdict.

### Outlook — deferred, not open work. Do not start any of these without the stated trigger

This is a *reference list of what stays dormant*, not a second remaining-work table — none
of these are being worked on, and none should be started speculatively. There is no further
backlog hiding elsewhere beyond this list.

| # | Item | Trigger |
|---|---|---|
| **M4** | **Regression spec** — `spec.py` + `verify/regression.py`, diff vs last-good across regenerations | A real estate part gets regenerated against a kernel/tolerance bump (the §3.1 ICP) |
| **M5** | **Backend dispatch** — generalize the two existing dispatchers (§11); do not write a third | Only if M3 proves consuming exported STLs is insufficient |
| — | **Generation loop** (`generate.py`, `loop.py`) | A consumer asks. The harness *verifies*; consumers' agents already write build123d and iterate by hand |
| — | **VLM annotation** (`annotate/`) | The render gate is in daily use and a human wants eye-drawing help. Advisory polish, never a gate. `AnnotateBackend` is a Protocol over the OpenAI-spec chat-with-images wire format; `../vlm-toolkit` is a provider behind it, not a second integration; `stub/none` always available |
| — | **Slicer printability** (`verify/slicer.py`) | A real print fails for a reason the gauge could have caught. Cheap port of estate code (§11), but nobody is blocked |
| — | **GUI-driven CAD** (`gui/`) | Never, unless a consumer needs browser CAD. Lowest ROI *and* lowest feasibility; explicitly cuttable |
| — | **CAE** (`verify/sim.py`) | **Research done** ([`reference/cae.md`](../reference/cae.md)) — and it moved CAE *further* out, not closer. A stress bound is not a gate-able check; only simulation-about-the-simulation checks (residual convergence, grid-convergence index) fit this project's stance. Revisit if a consumer has a real load case *and* accepts a human-judged, non-gating result |

---

# Part II — Build

Everything below is executable detail. Skip it until you are implementing.

## 6. M1 — Scaffold (no product code)

| File | Content | Notes |
|---|---|---|
| `pyproject.toml` | Python `==3.12.*` (§2), `uv`, Pydantic v2 + pydantic-settings, ruff (incl. `S`/`ANN`/`D`), pyright strict, pytest + pytest-cov + hypothesis, complexipy (max 15), scriv. `build123d` behind an optional `cad` extra; polyfetch env-borrowed, **not** a dep. `[project.scripts]` reserves `caxgauge = "caxgauge.cli:main"` — the entry point M2 fills and M3 adopts | Mirror `../so101-biolab-automation/pyproject.toml` structure |
| `Makefile` | `setup`, `lint`, `type`, `test`, `validate` targets | Mirror estate |
| `.github/workflows/` | `lint` (ruff + markdownlint + lychee), `test` (pytest, stub-mode, no network) | CodeFactor is a separate GitHub App integration, not a workflow — see the Handoff's Commands section for its current "Not supported" status |
| `AGENTS.md` + `CLAUDE.md` (redirect) | Governance; copy *structure* from so101, not content | |
| `.claude/rules/` | `core-principles`, `context-management`, `compound-learning` (estate SoT) **+ a new `verification-honesty.md`** | See below |
| `AGENT_LEARNINGS.md`, `AGENT_REQUESTS.md`, `CONTRIBUTING.md`, `CHANGELOG.md` | Estate standard | scriv or Keep-a-Changelog |
| `src/caxgauge/__init__.py`, `tests/` | Package skeleton mirroring `src/` 1:1 | Tool-dependent tests marker-gated, excluded by default. This is also the file that should make CodeFactor recognize the repo — verify that assumption once it lands |
| `.markdownlint.jsonc`, `lychee.toml` | Copy estate configs | `.gitignore` already landed |

**New rule `verification-honesty.md`** (day-one guardrail, encodes the §3.1 kill
conditions): green is *necessary, not sufficient*; the system never emits a
"verified/correct" verdict from deterministic checks alone; the VLM annotates and never
gates; no bounded auto-critique loop with revert-on-break; clearances are two-sided windows,
never min-only; an unsupplied bound reports `SKIP`, never `PASS`.

## 7. M2 — The honest gate (test-first)

### 7.1 Modules and their dependency graph

The file table below reads linearly, but the actual dependency graph has real parallelism.
Use this graph, not just the table order, when deciding what a solo builder does in sequence
versus what parallel subagents in separate worktrees can take concurrently (see the
Handoff's Execution model section):

```text
  Wave 0 (optional, single agent, ~5 min):
    verify/types.py  — PreflightResult, BrowserResult dataclasses/models.
    Defining these first lets waves 1-2 code against a fixed contract.

  Wave 1 (parallel — 2 worktrees, no shared dependencies):
    preflight.py            viewer/index.html
    (pure, zero deps)        (static three.js + STLLoader, no deps on preflight)

  Wave 2 (parallel — 2 worktrees, both depend only on viewer/index.html from wave 1):
    browser.py               render.py
    (load-check via the      (multi-view capture via the
     viewer)                  same viewer + polyfetch pattern)
    — do NOT start wave 2 until viewer/index.html exists; browser.py and render.py
      do not depend on each other.

  Wave 3 (single agent, needs preflight.py + render.py merged):
    report.py  — assembles PreflightResult + render output into one artifact

  Wave 4 (single agent, needs everything above merged):
    cli.py  — wiring only, no check logic. This is why it is built last regardless
              of how much of the above ran in parallel.
```

Merge each wave back to the integration branch before starting the next wave that depends on
it. Wave 1 and wave 2 are each internally parallel but sequential *relative to each other*
(wave 2 needs wave 1's `viewer/index.html`); waves 3 and 4 are strictly sequential
integration steps with no parallelism to extract.

`browser.py` and `render.py` are the only polyfetch-dependent pieces — see the separability
note in §9.

| File | Responsibility |
|---|---|
| `src/caxgauge/verify/preflight.py` | `preflight(mesh) -> PreflightResult`. Checks: **build-ok** (input parses to a solid/mesh), **watertight/manifold**, **bbox within envelope**, **volume/mass within band**. Structured per-check pass/fail, explicitly labelled necessary-not-sufficient. Pure — no agent, no network |
| `viewer/index.html` | Static three.js + STLLoader. Loads a mesh from `?mesh=`; named camera presets (front/iso/top/right). On load failure it must **throw** (fire `pageerror`) — do not rely on a readable success flag (isolated-world caveat, §9.2) |
| `src/caxgauge/verify/browser.py` | `browser_load_check(mesh) -> BrowserResult`. Serve the viewer on **`localhost`** (§9.1), load via env-borrowed polyfetch `render_session`, capture `console_errors` + `network_failures` + `pageerror`. A mesh that won't parse is a **hard fail**. Stub/SKIP when polyfetch/Chromium absent |
| `src/caxgauge/verify/render.py` | Multi-view PNG capture through `polyfetch` at the named camera presets. **The primary gate** — makes the human glance instant |
| `src/caxgauge/report.py` | Assemble render + preflight verdict into one human review artifact. Green shown as *necessary, not sufficient*; never the word "verified" |
| `src/caxgauge/cli.py` | `caxgauge check MESH [--envelope X,Y,Z] [--volume-band MIN,MAX] [--out DIR] [--no-render]`. Parse, call preflight + browser + render, write the report, exit non-zero on a hard fail. Thin — no check logic lives here |

**The CLI is the M3 contract.** M3's done-when is *"a consumer runs the gauge via a
documented command"*, so the command has to exist and be stable by the end of M2. Keep the
surface this small; new checks arrive as report content, not as flags.

**An unsupplied bound is `SKIP`, never `PASS`.** `--envelope` and `--volume-band` are
optional because most first runs will not have them, but a bbox check with no envelope must
report `SKIP` in the report and must not count toward green. Reporting "no bound given" as a
pass is the §3.1 false-confidence failure in miniature, reachable by default rather than by
gaming.

### 7.2 Deterministic checks — implementation notes

- **Watertight/manifold**: dependency-free binary-STL integrity via `struct` (reuse the
  pattern in `../so101-biolab-automation/src/hardware/slicer/validate.py:98-184` and
  `../i3mega-pipettebot/tools/slicer/validate.py`), plus OCCT `is_valid()` when build123d is
  installed.
- **Silent boolean-fusion feature loss** (the headline value): caught deterministically only
  via a **topology-count / volume-band** assertion against expectation. M2 scope: a fixture
  where a feature was absorbed must fail either the volume band or a face/edge count
  assertion. Full manifest-diff regression is M4, not here.
- **bbox / volume**: compute from the mesh; assert within envelope/band. Two-sided where
  applicable.

### 7.3 Fixtures and acceptance

Fixture-driven, no agent, no VLM. Fixtures under `tests/fixtures/` (kept out of the
`.gitignore` mesh exclusion deliberately).

1. **Good part** — passes all preflight checks; browser-load clean.
2. **Non-manifold mesh** — fails the watertight check; other checks may still run.
3. **Feature-absorbed mesh** (silent fusion loss) — fails the volume-band or topology-count
   assertion. ***This is the headline test.*** If it passes green, the preflight is
   worthless.
4. **Oversize part** — fails the bbox/envelope assertion.
5. **Un-parseable mesh** — `browser_load_check` returns a hard fail via `pageerror`; assert
   the failure is caught (force the failure to confirm listeners fire, §9.2).
6. **Honesty test** — a part that passes preflight but is visibly wrong must NOT yield a
   "verified" verdict.
7. **Unbounded-check test** — run the CLI with no `--envelope` and no `--volume-band`. The
   bbox and volume checks must report `SKIP`, the report must say which bounds were absent,
   and the run must not present as fully green.
8. **Stub-mode CI (primary gate)** — full path runs with no build123d, no Chromium; asserts
   the result shape and the necessary-not-sufficient labelling; `SKIP`s tool-dependent legs
   cleanly.

Property tests (Hypothesis): a generated box of size X passes its own bbox assertion and
fails a deliberately perturbed one.

## 8. M3 — Adoption

Run the gauge over real exported parts and put a report in front of a human.

- Source parts: `../so101-biolab-automation/src/hardware/parts.json` (20 parts) and
  `../i3mega-pipettebot/tools/cad/parts.json`, already exported to STL by their own
  `render.py`. No backend dispatch needed (§2, input contract).
- **Done-when:** one consumer repo runs `caxgauge check <part>.stl` (§7.1) on its real parts
  from its own Makefile or docs, and `so101-biolab-automation/docs/roadmap.md:47-58` is
  rewritten to point here.
- **Owner gate:** that roadmap rewrite is a cross-repo change to a repo with its own
  roadmap. Confirm before landing (§12.4).

## 9. Substrate — `polyfetch-scrape`

Rendering geometry headlessly is the awkward part of M2. Native VTK/OpenGL offscreen
rendering needs GL/X11 in CI and is fragile. Instead: render in a **minimal static three.js
page** and drive it with `../polyfetch-scrape` (Apache-2.0, Python ≥3.11, v0.7.0), which
owns Patchright/Chromium install, launch, teardown, capture and an SSRF guard.

What that buys: multi-view capture via `render_session(url)`; VP8 `.webm` turntable video
(`Response.video_path`); viewport / orientation / device emulation; a deterministic
browser-side gate via `page.on(...)`; and E2E UI testing of the report itself.
`render_session()` capture is **always on** — `s.console_errors` and `s.network_failures`
fill for the whole session. One-shot `fetch()` needs opt-in
(`RenderOptions(capture_console=True, capture_network_failures=True)`). `aria_snapshot()` is
the current API; `page.accessibility.snapshot()` was removed upstream. This makes
`browser.py` mostly wiring, not new logic.

**Consumption:** env-borrow via `uv run --directory ../polyfetch-scrape` (see its
`USING.md`) — no venv poisoning.

**Separability escape hatch.** Whether E2E UI testing of one's own app is in scope for
polyfetch at all is an open upstream question ([#144](https://github.com/qte77/polyfetch-scrape/issues/144);
its README positions it as a scraping toolkit). A "no" **does not block M2**: `preflight.py`
is pure and browser-free, so ship preflight + a manually-opened viewer and treat
`browser.py` / `render.py` as the separable piece needing a different substrate. The honesty
constraint still binds — preflight-only is not a shippable *gate* (§5), so the render must
arrive some way. Upstream issues filed 2026-07-20: docs caveat
[#180](https://github.com/qte77/polyfetch-scrape/issues/180), SSRF scope
[#181](https://github.com/qte77/polyfetch-scrape/issues/181).

### 9.1 Verified: use `localhost`, not `127.0.0.1`

Read directly at `../polyfetch-scrape/src/polyfetch_scrape/utils/_ssrf.py`. `check_ssrf(url)`
is **literal-IP only**: it parses the hostname and returns immediately if it is not a
literal IP. The module docstring states the scope — *"DNS names (incl. `localhost`) pass
through"*.

| URL | Result |
|---|---|
| `http://localhost:8080/viewer.html` | **Passes** — DNS name |
| `http://127.0.0.1:8080/viewer.html` | **Raises** — literal loopback IP |

Same destination, different outcome. This wastes an hour if rediscovered.

**Unverified inference — confirm empirically at M2.** Per `../polyfetch-scrape/docs/architecture.md`,
`check_ssrf` appears wired into `utils/sitemap.py` and `utils/discovery.py` only, not
`client.py` or the backends — so a direct `fetch()` / `render_session()` against a local
viewer is *probably* never guard-checked. That is inference from a doc table, not a read of
`client.py`.

### 9.2 Non-negotiable constraints

From `../polyfetch-scrape/AGENT_LEARNINGS.md`. Adopt from day one rather than rediscovering.

1. **Never assert three.js state via `page.evaluate`.** Patchright is a stealth Playwright
   fork running `evaluate` in an **isolated world**; page-script globals (`window.THREE`,
   module-scoped vars) read back `undefined` even when the scene rendered correctly. Their
   log records exactly this false negative. **Screenshots are ground truth for "did it
   render"; `page.on(...)` for "did it load".** Reserve `evaluate` for DOM you set or read
   structurally. This is the single highest-cost trap in the integration.
2. **Capture all four events.** Uncaught/parse errors fire `pageerror`, *not* `console`.
   Blocked or non-2xx resources fire `requestfailed` / non-200 `response`. Listening to
   `console` alone silently misses most real failures.
3. **A clean console means "no error on this network", not "no error".** Force a failure
   once and confirm the listeners fire.

## 10. Verification strategy

- **Fixture property tests** — §7.3.
- **Goodhart tests (from §3.1).** Assert the known gaming paths are *not* scored green: a
  part shrunk uniformly must fail a two-sided clearance window; a part with the offending
  feature deleted must not score better than the broken original — the report must surface
  the deletion, not hide it.
- **Necessary-not-sufficient honesty test.** Feed a part the preflight passes but that is
  visibly wrong (datum-offset hole). Assert the system **does not** report success — it
  presents the render for human decision and never emits a "verified/correct" verdict.
  Guards the false-confidence failure §3.1 calls worse-than-no-gauge.
- **Stub-mode CI (primary gate).** Runs end-to-end with no build123d, no OpenSCAD, no
  Chromium, no VLM; asserts the report is produced and green is labelled
  necessary-not-sufficient.
- **Golden-path integration (marker-gated).** One real part, real render capture.
- **Real acceptance test (M3).** The gauge runs over an existing part from
  `so101/src/hardware/parts.json` or `i3mega/tools/cad/parts.json` and produces a clean
  preflight + render a human accepts.
- **Regression test (M4).** Same part across a simulated kernel/tolerance shift: assert
  `verify/regression.py` surfaces the drift rather than silently passing or throwing a false
  failure.

## 11. Estate reuse — do not re-search for these

Exact paths verified 2026-07-20.

| Need | Source |
|---|---|
| **Stub-mode-by-default driver** — try real import, fall back to in-memory stub, log every command that would have been sent. Directly load-bearing: lets CI exercise full control flow with no CAD tooling installed | `../so101-biolab-automation/src/so101/arms.py:70-73,152-158,173-178` |
| **Binary-STL integrity via `struct`** + slicer-as-gate CLI wrap with first-class `SKIP` when the tool is absent. M2 reuses the `struct` check; the slicer gate itself is deferred | `../so101-biolab-automation/src/hardware/slicer/validate.py:1-275`; `../i3mega-pipettebot/tools/slicer/validate.py:77-132` |
| **Manifest-driven dual-backend dispatch** — declarative `{script, build_func, outputs, status}` registry. Two implementations to generalize from, **at M5, not before** | `../so101-biolab-automation/src/hardware/render.py:1-237` + `parts.json`; `../i3mega-pipettebot/tools/cad/render.py:99-133` + `parts.json` |
| **Coordinate-frame discipline** — CAD-frame vs machine-frame `+Y` mismatch bit them once and was promoted to an enforced rule. Adopt from day one | `../i3mega-pipettebot/.claude/rules/cad-script-conventions.md`, `AGENT_LEARNINGS.md:7-18`, example `tools/cad/labware/deck_plate.py:206-224` |
| **Policy-dispatch over class hierarchy** — `discover() -> classify() -> Policy (data) -> single dispatcher` | `../i3mega-pipettebot/docs/adr/0004-*.md:88-96` |
| **STEP round-trip escape hatch**; **config-over-code** (Pydantic `BaseSettings.from_yaml()`); **governance scaffold** (copy structure, not content) | `../so101-biolab-automation/docs/hardware/cad-tooling.md:125-145`; both repos; estate standard |
| **Render/browser substrate** (§9) · **VLM backend, deferred** — do not build a second VLM integration | `../polyfetch-scrape` v0.7.0 · `../vlm-toolkit` |

**Ruled out — do not re-investigate.** `RAPID-spec-forge` (archived; spec methodology is
*business requirements*, BDD guidance legacy and rejected); `VisAgent-Proto` (v0.0.0 draft);
`CellPlateVision-Prototype` (wrong domain).

**Research-hub relationship — `../ai-agents-research`.** It catalogs adopt/defer/skip
decisions about *AI coding agents and their ecosystems*, not domain knowledge. CAD/CAE
domain research does **not** belong there (it is in
[`../reference/landscape.md`](../reference/landscape.md)); harness-level findings do. Its
standing rule *"verify subagent findings before acting"* applies here — the scoping session
had two research agents die on rate limits and one return off-task text that looked like a
result.

## 12. Risks and open questions

1. **~~CAE leg unresearched.~~ Closed 2026-07-25** — [`reference/cae.md`](../reference/cae.md).
   It disproved this plan's own hypothesis (§4) that a CAE check is structurally a bbox
   check; CAE belongs on the render side of the trust boundary. It also returned independent
   empirical support for §3.1: across 1,120 controlled FEA trials, critic agents endorsed
   outputs — errors included — 85-92% of the time ([arXiv:2408.13406](https://arxiv.org/abs/2408.13406)). The red-team
   killed the auto-critique loop from first principles; this is the measurement.
2. **Live kill conditions** — the LOSES-IFF list in §3.1. The Goodhart + honesty tests in
   §10 exist to hold these off; if they are ever weakened or skipped, this risk is live.
3. **~~Ruleset required-check name is stale.~~ Checked live 2026-09-10 — already correct.**
   `gh api repos/qte77/agentic-cax-gauge/rulesets/19218207` shows `required_status_checks`
   context is `CodeFactor`, matching what CodeFactor actually posts. **Root cause found
   2026-09-18:** CodeFactor's own dashboard shows "Not supported" for this repo — it cannot
   analyze a repo with no recognizable source code, so the required check can structurally
   never post before M1 ships real Python files. This is why every PR since #1 has needed
   the `--admin` bypass, and will keep needing it until M1 lands. Separately: `bypass_actors`
   is empty on the ruleset, and a PR that was retargeted mid-flight (`gh pr edit --base`) can
   have its `--admin` bypass rejected (`Required status check "CodeFactor" is expected`) even
   when an otherwise-identical fresh PR against the same base merges cleanly — reproduced
   2026-09-18. Fix by closing and reopening rather than retargeting.
4. **M3 adoption is a cross-repo change.** Rewriting `so101-biolab-automation/docs/roadmap.md:47-58`
   touches a repo with its own roadmap — confirm with the owner before landing.
5. **polyfetch in-scope answer pending** — §9. Does not block M2 starting.
6. **AHA check.** The CAD-build/slicer pattern appears twice. Generalizing it *inside* this
   repo is fine; extracting a shared package consumed by all three is deliberately **not** in
   scope until the pattern proves stable here.
7. **~~The research sweep was incomplete.~~ Closed 2026-07-25** by a marketplace re-sweep
   ([landscape](../reference/landscape.md) §3.0 records coverage). Outcome that matters:
   `jdilla1277/agentcad` is on build123d doing deterministic checks + four-view render +
   human viewer — the same three moves as v0. **The technical wedge is gone; the only
   remaining differentiator is the honesty framing** (necessary-not-sufficient labelling,
   `SKIP`-not-`PASS`), which nobody else states. This does not change v0 — positioning was
   already estate-first (§2) and §3.1 already concluded there is no moat — but it converts
   the honesty rules from *principle* to *the entire product*. Weakening them now yields a
   redundant tool, not merely a weaker one. Re-sweep marketplaces, not arXiv, next time.
8. **"agentic" is a crowded prefix.** Accepted knowingly.
</content>
